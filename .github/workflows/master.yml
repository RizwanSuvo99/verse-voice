on:
  push:
    branches:
      - master
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 📂 Sync files
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ftp.classroomwriters.com
          username: crwfront@classroomwriters.com
          password: ;DuM?0Z6uHAQ
          server-dir: ./
          local-dir: ./
          exclude: |
            /lost+found/
            .git/
            node_modules/
